//const chalk = require("chalk"); //es2015 syntax prior to using babel preset latest
import chalk from "chalk"; //es6

console.log(chalk.green("Starting app in dev mode...")); // eslint-disable-line no-console
